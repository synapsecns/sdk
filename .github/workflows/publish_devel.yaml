name: Publish (Dev/Beta)

on:
  push:
    tags:
      - "v[01].[0-9]+.[0-9]+-dev.[0-9]+"
      - "v[01].[0-9]+.[0-9]+-beta.[0-9]+"
      - "v[01].[0-9]+.[0-9]+-alpha.[0-9]+"

jobs:
  tagged_release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/actions/publish-setup
        id: publish_setup

      - name: Import GPG key
        id: import_gpg
        uses: crazy-max/ghaction-import-gpg@v4
        with:
          gpg_private_key: ${{ secrets.BLEAKER_GPG_PRIVKEY }}
          passphrase:      ${{ secrets.BLEAKER_GPG_PASSPHRASE }}

      - uses: actions/setup-node@v3
        with:
          cache: "yarn"
          scope: "@synapseprotocol"
          registry-url: "https://registry.npmjs.org"
          node-version-file: '.nvmrc'

      - uses: ./.github/actions/yarn-cache
        with:
          node-version: ${{ steps.publish_setup.outputs.nvm-version }}

      - run: yarn run build

      - uses: ./.github/actions/publish-github-release
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          tarball-file: ${{ steps.publish_setup.outputs.tarball-file }}
          prerelease: true

      - name: Publish to NPM
        run: yarn publish --non-interactive --tag ${{ steps.publish_setup.outputs.npm-tag }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_SERVICE_ACCOUNT_TOKEN }}